import * as React from 'react';

class Registeration extends React.Component {
    render() {
        console.log("This is the registeration page to be built");
        return (
            <h1>This is the registeration page!</h1>
        );
    }
}

export default Registeration;